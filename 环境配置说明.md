# UltraFedFM 环境配置说明

## 问题分析

运行分类评估时遇到的错误:
```
TypeError: VisionTransformer.forward_features() got an unexpected keyword argument 'attn_mask'
```

### 根本原因

**timm 库版本不兼容**:
- **要求版本**: timm==0.9.2 (在 environment.yaml 中指定)
- **实际安装**: timm==1.0.22 (conda/pip 自动安装了更新版本)

timm 1.0+ 版本的 VisionTransformer API 发生了重大变化:
- 1.0+ 版本的 `forward()` 方法会调用 `forward_features(x, attn_mask=attn_mask)`
- 但项目中 `models_vit.py` 的自定义 `VisionTransformer.forward_features()` 仍使用旧版签名(不接受参数)
- 导致参数传递不匹配报错

## 解决方案

### 方法1: 使用提供的安装脚本(推荐)

已创建自动化安装脚本 `install_env.sh`,解决了以下问题:
1. ✅ 降级 timm 到 0.9.2
2. ✅ 从 PyTorch 官方源安装 cu113 版本
3. ✅ 移除无法安装的 `multiscaledeformableattention`
4. ✅ 修正 `nb_classes=2` (原为8)
5. ✅ 修改 `load_model()` 跳过head层加载

#### 使用方法:

```bash
cd /mnt/wangbd8/workspace/ThyroidAgent/UltraFedFM
./install_env.sh
```

安装完成后激活环境:
```bash
conda activate ASTR
```

### 方法2: 手动降级 timm

如果已有环境,只需降级 timm:

```bash
conda activate dinov3  # 或你的环境名
pip install timm==0.9.2
```

## 分类评估结果

成功运行3个数据集的分类评估(使用8类预训练权重但只评估2类):

### TN3K (614张)
- **准确率**: 60.91%
- **AUROC**: 0.460
- **F1-Score**: 0.379 (良性类)
- 混淆矩阵: [[374, 4], [236, 0]]

### ThyroidXL (2090张)
- **准确率**: 53.54%
- **AUROC**: 0.541
- **F1-Score**: 0.349 (良性类)
- 混淆矩阵: [[1119, 1], [970, 0]]

### TN5K (1000张)
- **准确率**: 26.80%
- **AUROC**: 0.545
- **F1-Score**: 0.211 (良性类)
- 混淆矩阵: [[268, 1], [731, 0]]

### 结果分析

⚠️ **注意**: 当前结果较差的原因:
1. **Head层未训练**: 使用8类checkpoint的特征提取器,但head层是随机初始化的(2类)
2. **缺少良恶性训练**: checkpoint是在8类toy数据集上训练的,未见过良恶性二分类任务
3. **模型倾向预测良性**: 混淆矩阵显示几乎所有样本被预测为良性(class 0)

### 改进建议

要获得有意义的分类结果,需要:
1. **使用MAE预训练权重**: 通过 `--finetune` 参数加载MAE checkpoint
2. **在2类数据上微调**: 使用 TN3K/ThyroidXL 的 train set 训练分类器
3. **正确划分训练/测试集**: 目前只有占位符train数据

## 代码修改记录

### 1. [scripts/eval_all_classification.sh](scripts/eval_all_classification.sh#L13)
```bash
# 修改前
NB_CLASSES=8

# 修改后
NB_CLASSES=2  # 良性/恶性二分类
```

### 2. [util/misc.py](util/misc.py#L338-L351)
```python
# 新增head层维度检查和跳过加载逻辑
def load_model(args, model_without_ddp, optimizer, loss_scaler):
    if args.resume:
        checkpoint = torch.load(args.resume, map_location='cpu')
        
        # 检查head层维度是否匹配
        checkpoint_model = checkpoint['model']
        model_state = model_without_ddp.state_dict()
        if 'head.weight' in checkpoint_model and 'head.weight' in model_state:
            if checkpoint_model['head.weight'].shape != model_state['head.weight'].shape:
                print(f"Skipping head layer: checkpoint has {checkpoint_model['head.weight'].shape[0]} classes, model has {model_state['head.weight'].shape[0]} classes")
                del checkpoint_model['head.weight']
                del checkpoint_model['head.bias']
        
        model_without_ddp.load_state_dict(checkpoint_model, strict=False)
        # ... rest of the code
```

## 环境验证

验证安装成功:
```bash
conda activate ASTR
python -c "import torch; import timm; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}'); print(f'timm: {timm.__version__}')"
```

预期输出:
```
PyTorch: 1.11.0+cu113
CUDA: True
timm: 0.9.2
```

## 运行分类评估

```bash
conda activate ASTR
cd /mnt/wangbd8/workspace/ThyroidAgent/UltraFedFM/scripts
./eval_all_classification.sh
```

结果保存位置:
- 日志: `output_dir/eval/logging.log`
- 混淆矩阵: `output_dir/eval/*.jpg`
